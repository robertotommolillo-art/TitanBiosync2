name = "titanbiosync-ai-coach"
main = "src/index.ts"
compatibility_date = "2024-01-01"
account_id = "dd8bb537d4400928fa635708d6f3013e"

[vars]
# APP_TOKEN is set via `wrangler secret put APP_TOKEN` (never hardcode secrets)
# OPENAI_API_KEY is set via `wrangler secret put OPENAI_API_KEY`
